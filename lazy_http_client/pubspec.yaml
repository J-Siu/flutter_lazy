name: lazy_http_client
description: Simple http client class that will auto apply specified headers on all requests.

version: 2.0.1
homepage: https://github.com/j-siu/flutter_lazy

environment:
  sdk: ^3.9.2

dependencies:
  http: ^1.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
