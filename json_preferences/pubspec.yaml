name: json_preferences
description: Combine json object with [SharedPreferences]. And an extended version with notifier.
version: 2.0.3
homepage: https://github.com/j-siu/flutter_lazy

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  lazy_log: ^2.0.2
  lazy_extensions: ^2.1.1
  # lazy_extensions:
  #   path: ../lazy_extensions
  # lazy_log:
  #   path: ../lazy_log
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
